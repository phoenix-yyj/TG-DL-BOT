# 项目协作要求

## 技术与运行

- 使用 Python 3.11+、`uv` 和 Pyrogram；依赖变更必须同步更新
  `pyproject.toml` 与 `uv.lock`。
- 本地测试使用 `uv run pytest`；修改行为时应补充或更新相应测试。
- Docker 镜像必须使用锁定依赖构建，并保持 `docker compose up --build` 可用。

## 安全与维护

- 不提交 `.env`、Telegram session、下载文件、日志或任何凭据。
- 优先进行小范围、可审查的改动；不要无关重构或覆盖已有用户改动。
- 修改命令处理器时，保留现有的 FloodWait、限流和资源清理行为，除非任务明确要求变更。

## Git 提交规范

- 每次任务完成后，按可独立验证的最小单元拆分提交并立即提交；不要将无关改动混入同一提交。
- 每个提交前运行与该改动相关的最小验证，并检查暂存区内容。
- 提交信息使用中文 Conventional Commit 格式：
  `<type>(<scope>): 中文标题>`；正文仅使用中文变更日志项目符号。
